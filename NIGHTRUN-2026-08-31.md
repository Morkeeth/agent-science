# NIGHTRUN 2026-08-31 — LANE L5, AGENT SCIENCE

## IDENTITY

Lane **L5 — Agent Science** (Agentic Cinema, Wed 9 Sep, $75K).
Repo `~/CODE/cleared` → `github.com/Morkeeth/agent-science`.
Branch **`night/l5-semantic-guard`**, pushed to origin. `main` untouched.
Wave 2, 01:35–03:0x.

Tasks A (semantic guard), B (C3 secret control), C (registry front surface) — all three
attempted, all three landed.

---

## STATE — pasted output

### The full suite, 16 files — one sweep, `for f in tests/test_*.py; do python3 $f; done`

```
test_adk_default_path.py                 5/5 passed
test_backfill_seeds_reuse.py             2/2 passed
test_clear_corpus.py                     4/4 passed
test_clearance_memo_html.py              5/5 passed
test_cross_subject_reuse.py              2/2 passed
test_partner_runtime.py                  5/5 passed
test_refusal_correctness.py              all passed
test_registry_surface.py                 13/13 passed
test_search_efficiency.py                3/3 passed
test_search_path.py                      5/5 passed
test_secret_surfaces.py                  6 passed, 0 failed
test_semantic_guard.py                   16 passed, 0 failed
test_source_map.py                       3/3 passed
test_stack_product.py                    stack tests OK
test_vision_wiring.py                    3/3 passed
test_watch_it_go_red.py                  72 passed, 0 failed
```

Plus, added in the depth pass after the closeout was first written:

```
test_guard_regression.py                 5 passed, 0 failed
test_semantic_guard.py                   18 passed, 0 failed   (was 16)
```

### A — the guard, watched RED first (`python3 tests/test_semantic_guard.py`, null guard)

```
  PASS  the shipping span for rc5 really is verbatim
  FAIL  guard refuses the rc5 span
        AssertionError: the guard admitted RC5: a span that says 'this Item is NOT one of them'
  FAIL  guard reaches rc5 through verify when the claim is passed
  PASS  guard keeps every labelled supported item
  PASS  a negation inside the claim is not a flip
  PASS  identical claim and span is never refused
  PASS  guard only demotes never rescues
  PASS  flag off restores the old behaviour exactly
  FAIL  named source path refuses rc5 with the guard on
  FAIL  search path refuses rc5 with the guard on
  PASS  guard carries no site specific chrome
9 passed, 4 failed
```

Four behavioural controls red for their own reason; the false-refusal controls green,
proving the suite can separate the two directions before either was built.

### A — measured (`python3 scripts/eval_semantic_guard.py`, 2026-08-31)

```
GOLD  fixtures/refusal-correctness/set.json  n=6  labelled 2026-08-22T21:30:00Z
  id    gold           guard OFF      guard ON
  RC1   SUPPORTED      SUPPORTED      SUPPORTED
  RC2   SUPPORTED      SUPPORTED      SUPPORTED
  RC3   NOT_SUPPORTED  NOT_SUPPORTED  NOT_SUPPORTED
  RC4   NOT_SUPPORTED  NOT_SUPPORTED  NOT_SUPPORTED
  RC5   NOT_SUPPORTED  SUPPORTED      NOT_SUPPORTED  *closed*
  RC6   SUPPORTED      SUPPORTED      SUPPORTED
  correct: guard OFF 5/6   guard ON 6/6

ATTRIBUTION  over the 27 GREEN verdicts the shipping engine produces on research-corpus/
  polarity  fires on   1/27
  binding   fires on  15/27
  coverage  fires on  12/27
  coverage threshold sweep: 0.30 -> 8/27  0.40 -> 12/27  0.50 -> 14/27  0.60 -> 17/27

REGISTRY  research-corpus/, n=314 claims, offline replay   [re-run 03:2x, after the aside fix]
  guard OFF  {'sourced': 27, 'refused': 271, 'unknown': 16}
  guard ON   {'sourced': 27, 'refused': 271, 'unknown': 16}
  verdicts changed: 0/314 (0.0%)
  refusals RESCUED to SOURCED: 0 (must be 0 — the guard may only demote)
  SOURCED on a DIFFERENT span: 8/27 — same verdict, better evidence
```

**1 false GREEN closed, 0 true GREENs lost, 0/314 verdicts changed, 8/27 spans improved.**

The guard was changed once more after the first measurement (the relative-clause aside,
below), so the whole eval was re-run rather than the old numbers reused. The population
moved too: the first read was n=313, the re-run n=314, because another lane wrote a file
into `research-corpus/` mid-run. Both readings stated; the shipped ones are the later.

### A — the submission's eval gate moved

`python3 scripts/eval_refusal_baseline.py` and `..._ablation.py`, re-run 2026-08-31:

```
Baseline:  5/6 = 0.833  95% CI [0.436, 0.970]
Shipping:  6/6 = 1.000  95% CI [0.610, 1.000]
Delta (shipping - baseline): +1
McNemar:   p=1.0000 (b=0 c=1 discordant)
```

Where 2026-08-30 measured a **tie with delta=0**, there is now a **+1 delta**. Carried
with its worst number: **p=1.0000, not significant at n=6, CIs overlap heavily.** One
discordant item is a direction, not a result.

### B — C3, watched RED against the shipped rule (`python3 tests/test_secret_surfaces.py`)

```
  FAIL  every way to leak a secret is caught
        3 of 7 leak shapes pass the scanner:
        - Cloud Run / k8s service yaml — the declarative form of the same command
        - docker run -e
        - docker compose environment list
  PASS  the safe forms are not flagged
  PASS  the live tree is clean
  FAIL  the control and its control share one rule
4 passed, 2 failed
```

then, after the fix: **`6 passed, 0 failed`** — 7/7 leak shapes caught, 0/4 safe forms
flagged.

### C — the registry surface, rendered and looked at

`ask_registry.render_page()` → 85,878 bytes; screenshotted headless at 1280×2400 and
390×1400 and read. Live counters from the DB, read 02:05: **177 claims · 28 sourced · 149 refused ·
12 thin evidence · 0 reuses · 2 productions.** Re-read at 03:0x it is **1 reuse** — the
queries I ran while rendering and screenshotting the page moved it, which is what the
counter is for. Stated with its read time because a live number quoted without one is
the thing this lane spent the night removing.

---

## VERIFIED

- Every test count above was run in this session and pasted from its own output.
- The guard's red lights were **observed before** the guard existed — twice: once as an
  ImportError (rejected as not a real red), then against a fully wired, completely inert
  null guard, which is also the ablation arm.
- The C3 gaps were observed red against the **shipped** rule, transcribed verbatim into
  `review/secret_surfaces.py` before a character of it was changed.
- The registry page was **rendered and viewed**, not asserted. Looking is what found the
  thin-evidence defect: SOURCED rows quoting navigation furniture (the Galileo row's
  evidence was `"Solutions in 2026 | Galileo ')"> Platform Docs Pricing Resources
  About Login…"`).
- RC5's promotion out of `engine_limit` was performed on the instruction its own defect-pin
  printed when it fired.
- `hack.md`, `docs/QWEN-EVAL-GATE-2026-08-30.md` and
  `docs/FINDING-substring-is-not-a-statement.md` were updated because they carried claims
  this run contradicts. The 08-30 numbers are kept as the dated record of that run, not
  rewritten.

## UNVERIFIED

- **Nothing was run against the hosted URL.** Every number here is local. The hosted
  revision does not have this branch.
- **The 313-claim registry replay is UNLABELLED.** It is not a gold set. Every changed row
  was printed and adjudicated by hand; two false refusals found that way were fixed and
  pinned, and the final run changed 0 verdicts. That is a weaker instrument than labels.
- **n=6 is the only labelled population.** 6/6 on six items, and the eval's own McNemar is
  p=1.0000. The guard is not proven; it is measured, in both directions, on what exists.
- **The guard is not comprehension.** Three narrow checks over negation and subject
  binding. It will not catch a false GREEN that is fluent, positively phrased, and simply
  about a different fact.
- **A count bug shipped and was caught in review, not by me.** The UNSOURCED filter
  reported `total = len(rows)` after the SQL LIMIT, so the shelf header read "120 claims
  matching" one click from a counter reading "149 refused" — tonight's exact failure
  class, live on the surface built to prevent it. Fixed in SQL, bound by a test that seeds
  140 rows against a 120-row page. **I rendered that page and looked at it and did not see
  it**, because I looked at the default view and not at the filter.

- **The thin-evidence residue is open.** 12 of 28 sourced rows rest on a span carrying
  under 40% of the claim's terms. Marked and counted on the surface; not refused, because
  a coverage gate also refuses honest paraphrase (measured — it refuses this product's own
  canonical claim).

## BLOCKED ON OSCAR

1. **ROTATE THE PARALLEL AND GEMINI KEYS. Open 8 days.** Two keys sit in plaintext in a
   Cloud Run revision, its config and its build logs. Those cannot be un-written and
   nothing in this run touches them. C3's control now catches 7/7 leak shapes going
   forward; it cannot undo the one that already ran.
2. **Deploy** — `bash deploy.sh`. The hosted desk does not have this branch.
3. **Submission gates** — public repo, video, Devpost.
4. **C2 (the redeploy) was SKIPPED as instructed**, gated on the rotation.
5. **Merge `night/l5-semantic-guard` → `main`.** Not done: only ReadyCounter may push main
   tonight.

## DECISIONS

1. **The gate is `polarity` only.** `binding` and `coverage` were built, measured, and cut
   as gates because both refuse this product's own canonical claim — a fair paraphrase
   whose subject sits in the previous sentence. Kept, exported, measured, and `coverage`
   redeployed where it is sound: ranking spans, and flagging thin evidence on the surface.
2. **Default ON**, `CLEARANCE_SEMANTIC_GUARD=0` restores the 2026-08-30 engine exactly.
   Decided after the measurement, not before.
3. **The locator now yields candidates, not one guess.** Forced by the measurement: the
   first arm read 5/6 → 4/6, and reading the spans showed RC1 and RC2 had been GREEN on a
   `<nav>` link and on the wrong sentence. The set could not see it because it labels
   whether a claim is supported and never which span supported it.
4. **RC5 promoted out of `engine_limit`,** replaced by a harder control: the set must carry
   no engine limits, AND the flag-off arm must still reproduce the false GREEN.
5. **C3 was audited, not rebuilt** — the control already existed.
6. **The curve is a cited exhibit, never a live counter** (`clearance/curve.py`), with the
   live-vs-cited separation unconditional. The first version only spoke when reuse was 0,
   so it switched itself off the moment anyone used the desk.

## FOUR DEFECTS FOUND BY MEASURING, NOT BY THINKING

1. RC1/RC2 were false GREENs **inside the labelled-correct column** of the fixture written
   to catch false GREENs.
2. `_start_of_statement` required a capital followed by a lowercase word, so **"Member
   States shall bring into force…" could only start at "States"** — an EU directive's
   operative provision quoted from its second word. The look-back window was also shorter
   than the sentence it was trying to find the start of.
3. **"not A but B" is a correction, not a denial** — and the carrier clause was being found
   by substring, which fails whenever `must_contain` ends on punctuation the clause
   splitter has just consumed.
4. **`fails`, `lacks`, `unable` were in a list the module calls a closed class of function
   words.** They are lexical verbs. The list contradicted its own stated principle.

Plus, in the repo's existing controls: **the C3 scanner's own control graded a copy of the
rule**, and **two registry tests were pinned to copy rather than to properties** and went
red on a page that shows strictly more than it used to.

## NEXT

1. Merge to `main` after Oscar's review (or ReadyCounter).
2. `bash deploy.sh` once keys are rotated → the hosted `/registry` gets this surface.
3. **A fifth guard defect was found by the regression set on its first run** — a negation
   inside a non-restrictive relative clause ("The Act, which had **not** been amended,
   came into force on 1 April 2024") was read as denying the main predication. Fixed,
   both directions pinned. The set is DERIVED and marked `held_out: false`; a control
   fails if anyone merges it into the n=6 held-out set.
4. **The thin-evidence residue is the next real build.** 12 of 28 rows quote page furniture
   because the locator had no better sentence in that document. The fix is a locator that
   can say "this document does not contain a sentence that states this" and refuse — which
   is a different mechanism from the guard, and needs its own labelled set.
5. Grow the labelled set past n=6. Every honest number in this lane is bounded by it.
6. Feed the surface a second production so the reuse counter measures something real.

## RE-ENTRY PROMPT

> Lane L5, Agent Science, `~/CODE/cleared`, branch `night/l5-semantic-guard` (3 commits,
> pushed, `main` untouched). The RC5 substring false-GREEN is CLOSED by
> `clearance/semantic.py` — read `docs/FINDING-semantic-guard-2026-08-31.md` first; it
> carries the measurement in both directions and the two design changes the measurement
> forced. Gate is `polarity` only; `binding` and `coverage` were measured and cut as gates
> because they refuse the product's own canonical claim. Run
> `python3 scripts/eval_semantic_guard.py` to reproduce (gold 5/6→6/6, registry 0/313
> verdicts changed, 8/27 better spans) and `for f in tests/test_*.py; do python3 $f; done`
> for the 16-file suite. The open technical work is the THIN EVIDENCE residue: 12 of 28
> sourced rows quote page furniture because no better sentence exists in that document —
> visible on `/registry` under the "thin evidence" filter. Blocked on Oscar: key rotation
> (8 days), deploy, merge to main, submission gates.

## RUNTIME + STOP REASON

Started ~01:35, closeout written ~03:0x. Roughly 90 minutes.

**Stop reason: all three assigned tasks landed, measured, committed and pushed; closeout
written.** Not budget, not a blocker. Continuing into adjacent depth per standing law 1 —
the thin-evidence residue named in NEXT is the honest next object, and it is in this repo,
not a new one.

---

# WAVE 4 — THE AMBITION WAVE · L5 Agent Science · the registry gets a face

**~04:25 → ~07:45. Branch `night/l5-semantic-guard`. `main` untouched.**

## THE HEADLINE: the brief's own killer demo was false, and the truth was better

The lane brief said the engine already REFUSED "Article 50 fines = €35M/7%" via
`number_not_bound_to_subject`. **Neither existed.** The check is nowhere in the repo, and
the shipping engine **cleared the claim GREEN** against the live EUR-Lex text of the AI
Act at 04:40:

```
$ python3 -c "...judge_claim(Claim('A50', CLAIM, URL, mc), fetch=True)"
GREEN None
span: "Non-compliance with the prohibition of the AI practices referred to in Article 5
shall be subject to administrative fines of up to EUR 35 000 000 or, if the offender is
an undertaking, up to 7 % of its total worldwide annual turnover ..."
```

That is Article 99(3), about **Article 5**. Article 50 is Article 99(4)(g) at **EUR
15 000 000 / 3 %**. Verbatim, in the cited document, carrying **73 %** of the claim's
content terms. Every test a keyword grounder runs, it passes. The error is also in this
repo's own `research-corpus/2026-08-24-verification-stack-positioning.md`.

**Law §4, twelfth instance tonight — and the first one found inside the instruction
rather than inside a product.** The brief was not rendered. The mechanism was built so
the engine does what the brief claimed, and then the engine's own output was rendered.

## SHIPPED

1. **`cited_provision_differs`** — `clearance/semantic.check_citation`, the fourth
   mechanism, shipping in `DEFAULT_CHECKS`. A provision citation is two tokens meaning
   one thing; `content()` drops tokens of length 1, so the `5` in `Article 5` never
   survives tokenisation and `article` matches `article`. Not a weak check — the **wrong
   object**. Parsed from raw text, closed class of structural heads, gated on CONFLICT
   only.
2. **The audit trail.** `Verdict.refusal_code` + `Verdict.trail`; `_admissible` records
   every candidate span, whether it was admissible, the code and detail that killed it,
   and its coverage — threaded through all three doors into GREEN, not one.
   Persisted additively in `refusal_log` (`ALTER TABLE`, NULL for old rows), written by
   **named column** so schema growth cannot shift thirteen positional values.
3. **The front surface** — `/front`, `ask_registry.render_front`. The wedge in the first
   ten seconds, the curve as two panels, the negative space counted.
4. **`/refusal?term=`** — a refusal you can open: the claim, every span weighed, and the
   exact finding that killed each one.
5. **`clearance/wedge.py` + `scripts/wedge_receipt.py`** — the exhibit holds INPUTS and
   provenance only. No verdict, no span, no number. A control greps the module for them.
6. **35 new controls** (`test_citation_conflict.py` 21, `test_front_surface.py` 14), every
   one written and run RED first.

## THE MEASUREMENT, AND THE LIMIT THAT MATTERS MORE

`python3 scripts/eval_citation_conflict.py`

```
GOLD  n=6, labelled 2026-08-22 :  BASE 6/6   CONFLICT 6/6   ABSENCE 6/6
ATTRIBUTION  over the 27 GREEN verdicts on research-corpus/,
             0 of which cite a provision at all
REGISTRY  314 claims:  CONFLICT changes 0 verdict(s) vs BASE
WEDGE  WEDGE-1 BASE GREEN -> SHIPS UNKNOWN cited_provision_differs  OK
       WEDGE-2 BASE GREEN -> SHIPS GREEN                            OK
```

**"Measured on 314 claims, zero false refusals" is true and is the wrong object.** Zero of
the 27 cleared claims cite a provision, so the population contains no case the check could
have failed. That reads exactly like a safety result and is not one. The front page prints
the eligible-row count FIRST and says the corpus cannot exercise the mechanism —
`t_the_measurement_strip_leads_with_eligible_rows_not_verdicts_changed` fails the build if
that sentence is ever removed. The ABSENCE arm's 6/6 is therefore **not** an argument to
ship it, and it does not ship.

## FIVE DEFECTS FOUND BY RUNNING IT, NOT BY THINKING

1. **The lane brief's killer demo did not reproduce** — the engine cleared the claim.
2. **An exclusion is not a subject.** The gate's first run refused the TRUE claim: Article
   99(4) names Article 5 in order to *exclude* it (`other than those laid down in Articles
   5`). Closed by `_EXCLUSION`.
3. **The control arm was not a control arm.** `inspect(checks=DEFAULT_CHECKS)` binds the
   default at import, so the eval's BASE arm changed nothing and silently ran the
   treatment. Caught by a receipt printing `BASE=REFUSED` for a case measured GREEN
   twenty minutes earlier.
4. **The mark asserted a fault on the row that was right.** The near-miss device boxed
   EVERY provision in refusal-red — including `Articles 5` inside the span that CLEARS the
   true claim. Two correct facts adjacent, asserting a relation neither supports. Marks
   now come only from the engine's own finding; `_rivals_named_by` also had to stop
   swallowing `and never Article 50` from the end of that finding.
5. **`ask_registry --serve` had never worked.** `serve()` referenced `_Handler`, which did
   not exist — `NameError` on every invocation, for a week. Found only because rendering
   the front surface meant someone finally ran it.

## THE PHONE CHECK, DONE THE WAY LAW §5 REQUIRES

Playwright device emulation, measured **inside the page**:

```
front:phone   clientWidth 390  innerWidth 390  bodyScrollWidth 390
front:desk    clientWidth 1440 innerWidth 1440 bodyScrollWidth 1440
refusal:phone clientWidth 390  innerWidth 390  bodyScrollWidth 390
```

It caught two real defects a cropped screenshot would have hidden: the causes table pushed
the body to **411px** (fixed — the table scrolls inside its own box), and the near-miss
grid held its 8.5rem label column at 390px, leaving the span **fourteen characters wide**
(fixed — a phone breakpoint stacks the two readings).

## SUITE

```
$ python3 tests/test_watch_it_go_red.py     72 passed, 0 failed
$ for f in tests/test_*.py; do python3 $f; done
  18 files, all green (test_citation_conflict 21/21, test_front_surface 14/14,
  test_semantic_guard 18/18, test_registry_surface 13/13, test_vision_wiring 3/3)
```

## BLOCKED ON OSCAR

Merge to `main` · `bash deploy.sh` after key rotation (9 days) · submission.
Nothing outward was done. The repo is still private and nothing was deployed.

## THE HONEST NEXT OBJECT

A **labelled set for this mechanism**: n=2 and one of them is the motivating case. Every
honest number in this lane is bounded by that. Second: a corpus that contains citations at
all — `research-corpus/` is an AI-industry corpus and has almost none, which is why the
314-claim replay could say nothing.

## CLOSEOUT CORRECTIONS — five more, four of them found after the first commit

A cross-check on the wave's own output found five things the wave's own controls did not.
Two are this night's defect class, in this wave's output.

1. **CORRECTION to the commit message above.** It says "35 new controls, every one run RED
   first." That is true of the **21** in `test_citation_conflict.py`, which were written
   and run red before `check_citation` existed. It is **not** true of the **14** in
   `test_front_surface.py`: those were written after the fixes, as pins, and their first
   run was 12/14 green with both failures being test bugs rather than observed defects.
   The mark defect *was* observed red — in pixels, on a screenshot, not in a test. In a
   repo whose guard file opens with "every test in this file was run RED before the
   module existed", that overclaim is the thing the whole night hunts. The commit is
   pushed and is not being rewritten; this is the correction of record.
2. **The hero's one call to action dead-ended on a cold clone.** `cache/refusal_log.db` is
   gitignored, so a fresh clone has the committed receipt and an empty shelf — and "open
   this refusal →", the link the brief calls the product, returned "Not on the shelf" for
   exactly the reader it was written for. It works on this machine, which is why nothing
   caught it. `render_refusal` now falls back to the committed **receipt**, which is
   engine output, so the no-hand-written-verdicts law still holds.
   Pinned: `t_the_heros_call_to_action_works_on_a_cold_clone`.
3. **Two SKIP messages named a script that does not exist.** They told an offline reader to
   run `scripts/seed_wedge.py`; the file is `scripts/wedge_receipt.py`. A cold clone's own
   test suite printed a sentence the repo contradicts. Pinned by a control that walks every
   `scripts/*.py` reference in `tests/` and `scripts/` and asserts the file exists.
4. **"this engine at 04:00 today" would rot in 24 hours** into a sentence the receipt's own
   `produced_at` contradicts. Now dated from the receipt.
   Pinned: `t_the_hero_never_says_today`.
5. **The page's prose called the cost curve "flat" while the chart drawn from those same
   four numbers showed the spread.** `curve.WHAT_IS_NOT_TRUE` read "$0.00377 → $0.00352,
   flat" one paragraph below a panel printing "Range $0.0031–$0.0040; last leg 7% below the
   first, and not monotonic". Two sentences, one set, disagreeing — on the front page of the
   product that exists to catch that. The surviving claim is that there is **no downward
   trend** while reuse climbs 46 points, so that is what it says now, with the band printed:
   *"$0.00309–$0.00396 across the four legs, with the last 7% below the first and the trend
   not monotonic."* Pinned: `t_nothing_on_the_page_calls_the_cost_curve_flat`.

Final: **19 test files green**, `test_watch_it_go_red.py` **72/72**,
`test_citation_conflict.py` 21/21, `test_front_surface.py` 17/17.
Phone re-measured after every change: `clientWidth 390 / bodyScrollWidth 390`.

---

# WAVE 4 · L5 — ADVERSARIAL VERIFICATION PASS (~07:50 → ~09:20)

A second session took the lane's claim and tried to break it at the object. Every number
below was re-run in this session; nothing is quoted from the lane's own report.

## WHAT HELD

| claim | how it was checked | result |
|---|---|---|
| suite 72/72, 19 files green | re-ran every `tests/test_*.py` | **held** — 21/21, 17/17, 18/18, 13/13, 3/3 |
| the eval's numbers | re-ran `scripts/eval_citation_conflict.py` | **held**, and `docs/EVAL-…json` came back **byte-identical** (`git diff` empty) |
| **the legal headline** | fetched the EUR-Lex text and read Article 99 | **held at the source** — see below |
| 73 % coverage | receipt's own `coverage` = `0.7272727…` | **held**, engine-computed |
| `DEFAULT_CHECKS` ships conflict, not absence | `('polarity','citation')` | **held** |
| trail written by named column | `record()` builds `cols`/`vals` in parallel | **held** |
| 177 of 179 rows carry no trail | `sqlite3` on `cache/refusal_log.db` | **held** exactly |
| the curve's numbers | recomputed all of them from `LEGS` | **held** — 56, 15/56=27 %, 0/20/39/46, 0.00352/0.00377 = 6.6 % → "7 %", 0.73→0.69 |
| phone 390 on `/front` and `/refusal` | Playwright iPhone 12 Pro, measured in-page | **held** — 390/390/390 |

The headline is TRUE, verified against the fetched document rather than against the lane:

```
$ grep the fetched EUR-Lex text (590,271 chars)
CHAPTER XII PENALTIES  Article 99 Penalties
  3. Non-compliance with the prohibition of the AI practices referred to in Article 5
     shall be subject to administrative fines of up to EUR 35 000 000 ... 7 % ...
  4. Non-compliance with any of the following provisions ... other than those laid down
     in Articles 5, shall be subject to administrative fines of up to EUR 15 000 000
     ... 3 % ... (g) transparency obligations for providers and deployers pursuant to
     Article 50.
```

## SEVEN DEFECTS, FIXED IN PLACE

1. **`/front` was a 404.** The lane brief, this file, the FINDING and the footer of
   `/registry` all name the route; `_Handler` answered `/`, `/index.html`, `/refusal`,
   `/registry` and nothing else. It is defect 5 of the wave above — `serve()` calling a
   `_Handler` that did not exist — recurring one level down, for the same reason:
   *rendering a page is not requesting it*. Fixed, and pinned by
   `t_every_internal_link_resolves_to_a_served_route`, which starts the real server and
   GETs every internal href the pages emit. **Run RED first: `/front -> 404 (linked from
   /registry)`.**
2. **"nothing on this page is written by hand" was false, on the exhibit that sells
   against exactly that.** `wedge.CASES[].note` renders beside each reading and asserts
   *"Article 99(4) sets EUR 15 000 000 / 3 % and reaches Article 50 at point (g)"*; so
   does a sentence `_wedge_html` appends after it. Both are TRUE — the provenance claim
   about them was not. `wedge.py`'s own docstring ("holds no verdict, no span and no
   number") was wrong the same way, and the grep control passed because it greps three
   literals rather than the class. `PROVENANCE` now scopes itself and names the
   hand-written reading; pinned by `t_the_provenance_line_does_not_overclaim`, **run RED
   first**.
3. **"this shelf" named two different populations one screen apart.** The measurement
   paragraph counted `research-corpus/` (314 claims, 27 cleared) and called it *the live
   shelf*; a thousand pixels lower the counters print the registry database (**179**
   claims, **29** sourced) and call that the shelf. Two correct numbers under one noun —
   the adjacency defect `clearance/curve.py` has a whole docstring about, committed one
   paragraph from the disclaimer that prevented it for the curve. The population is now
   named on the page.
4. **The shelf was never phone-checked — only the front page was.** Real device
   emulation on `/registry`: `clientWidth 390, bodyScrollWidth 486` — a 96px horizontal
   scroll on the one page the front surface sends a stranger to. Fixed (`overflow-wrap`
   on the prose rows) and re-measured at 390/390/390. A second, invisible to any
   measurement and visible only on the screenshot: the two-word filter chip *thin
   evidence* wrapped so its box spanned 21→390px and its underline ran flush into the
   viewport edge while `bodyScrollWidth` stayed a clean 390. Fixed.
5. **`check_citation`'s docstring said the span carried `75%%`** where the engine
   measures 0.7273 and every surface prints 73 %. Corrected.
6. **`docs/FINDING-…md` said `test_front_surface.py` = 14 controls**; it was 17 before
   this pass and is 19 now. Corrected.
7. **"the other nine causes" was a hard literal** beside a count derived from
   `len(CAUSE_ENGLISH)`. A tenth cause would have rendered *"the other nine … a closed
   set of 11"* — the warn box contradicting itself. Derived.

## THE MEASUREMENT THE LANE SAID DID NOT EXIST — AND THE FALSE GREEN IT FOUND

The lane closed saying its evidence was n=2 because `research-corpus/` contains no
provision-bearing claims. So this pass built that population: **11 claims against the same
Regulation, labelled before any run**, 8 true and 3 false near-misses.
`scripts/probe_citation_heldout.py`:

```
SHIPS 9/11   BASE 7/11      (BASE = polarity only, SHIPS = polarity + citation)
F1  REFUSED  BASE GREEN  SHIPS UNKNOWN  cited_provision_differs   OK
F2  REFUSED  BASE GREEN  SHIPS UNKNOWN  cited_provision_differs   OK
F3  REFUSED  BASE GREEN  SHIPS GREEN                              WRONG   <-- live false GREEN
T3  SOURCED  BASE UNKNOWN SHIPS UNKNOWN                           WRONG   <-- pre-existing, both arms
```

**The gate introduced ZERO false refusals** on rows that can exercise it, and closed two
of three. That is the strongest evidence the mechanism has, and it is not the lane's.

**F3 is the finding.** *"Article 5 breaches attract 'fines not exceeding 3 % of their
annual total worldwide turnover'"* is false — Article 5 is the 35M/7 % tier — and the
shipping engine clears it **GREEN** on Article 101(1), a paragraph about general-purpose
AI models. WEDGE-1 again, same document, and the shipped gate is silent:

```
provisions(claim) = [('article','5')]   carrier provisions = []   conflict = None
absence = cited_provision_absent: the claim is about Article 5 and the span never names it
```

**The recall boundary, stated exactly: the gate closes the error only when the rival
provision is cited BY NUMBER inside the carrier clause.** Article 101 names its subject in
words, so there is no numeral to conflict with. The pitch must say *"refuses this shape of
the error"*, never *"refuses the error"*. This is not an argument to ship ABSENCE; it is
the number the held-out set was supposed to produce.

**A second boundary, in the parser.** `provisions("Annex III") -> []`,
`provisions("Articles 8-17") -> [('article','8')]`. Roman-numbered provisions are
invisible, and in this exhibit's own instrument the high-risk list is **Annex III** and the
technical documentation is **Annex IV**.

**A case was withdrawn and the wrong label was mine**, not the engine's: `F4` asserted
"Annex IV sets out the technical documentation … for GPAI models" and was labelled
REFUSED; the span the engine returned *is* Annex IV, so GREEN is right. It is recorded in
the probe rather than deleted. The reportable score is **9/11 vs 7/11**, not 9/12 vs 7/12.

## REPORTED, NOT FIXED — the eval's population is a live baseline

`clearance/ingest.py::append_markdown` writes dated claim files **into
`research-corpus/`**, the same directory `eval_citation_conflict.py` replays as its
measurement population. Two were sitting untracked when the eval ran:

```
$ git status --short research-corpus/
?? research-corpus/2026-08-30-claim.md
?? research-corpus/2026-08-31-claim.md
$ # moved aside:  CLEAN-CHECKOUT total 312 sourced 27 refused 269 unknown 16
```

**From a clean checkout the REGISTRY line reads 312, not 314.** Every conclusion is
untouched — still 27 cleared, still 0 citing a provision, still 0 flips — but a published
denominator that any `ingest_claim()` call moves is a live baseline, not a frozen one. The
fix is a frozen manifest for the eval population, separate from the ingest sink; it is a
design change, not a small safe one, so it is written down rather than done.

## FINAL STATE, ALL RE-RUN IN THIS SESSION

```
$ for f in tests/test_*.py; do python3 $f; done      19 files, all green
    test_citation_conflict 21/21 · test_front_surface 19/19 · test_semantic_guard 18/18
    test_registry_surface 13/13 · test_watch_it_go_red 72/72
$ python3 scripts/eval_citation_conflict.py          unchanged; EVAL json byte-identical
$ python3 scripts/probe_citation_heldout.py          SHIPS 9/11   BASE 7/11

Playwright device emulation, measured inside the page:
  front:phone       clientWidth 390  innerWidth 390  bodyScrollWidth 390
  /front:phone      clientWidth 390  innerWidth 390  bodyScrollWidth 390   (was 404)
  registry:phone    clientWidth 390  innerWidth 390  bodyScrollWidth 390   (was 486)
  refusal:phone     clientWidth 390  innerWidth 390  bodyScrollWidth 390
  all four at desk  clientWidth 1440 innerWidth 1440 bodyScrollWidth 1440
  zero elements flush to the viewport edge on any of the eight renders
```

**VERDICT: ACCEPTED.** The headline is true at its source, the mechanism is real, the
measurement reproduces byte-identical, and the lane's own statement of its limits was
honest and understated rather than overstated. Nothing outward was done; `main` untouched.

---

# WAVE 5 · L5 — THE REPRODUCIBILITY ROOT CAUSE, CLOSED (~05:30 → ~06:30)

The wave-4 verifier reported one architectural defect and did not fix it: **"REGISTRY 314
claims" did not reproduce from a clean checkout.** This wave closed it, and found two more
on the way, one of them on this lane's own front page.

## THE DEFECT, STATED AT THE OBJECT

`clearance/ingest.py::append_markdown` wrote dated claim files **into `research-corpus/`**
— the same directory `eval_citation_conflict.py` and `eval_semantic_guard.py` replay as
their measurement population. Using the product moved its own published denominator:

| reading | when | why |
|---|---|---|
| n=313 | first run, ~02:xx | — |
| n=314 | re-run ~03:2x | a parallel lane called `ingest_claim()` mid-measurement |
| n=312 | clean checkout | two of "the 314" were untracked files the product had written to itself |

A measurement instrument contaminated by its own operation, in a product whose entire
pitch is that a number must name its population.

## THE CONTROL WAS WRITTEN FIRST AND WATCHED GO RED

```
$ python3 tests/test_frozen_population.py        # ~05:50, against the 074eef9 layout
FAIL  test_using_the_product_does_not_move_its_own_denominator:
      one append_markdown() call changed the frozen population: 312 -> 313 claims
      (extra: ['2026-08-31-t-frozen-population-probe.md'])
FAIL  test_ingest_does_not_write_into_the_measurement_population:
      ingest writes into the measured population: /Users/morkeeth/CODE/cleared/research-corpus
FAIL  test_no_published_number_is_computed_over_the_live_sink:
      ['eval_citation_conflict.py', 'eval_semantic_guard.py', 'clear_corpus.py']
FAIL  test_the_written_instructions_point_at_the_sink_not_the_population:
      AGENTS.md tells a writer to append to the FROZEN population
FAIL  test_the_evals_resolve_their_population_through_the_frozen_gate
3/8 passed
```

The behavioural one is the load-bearing control: it calls the real writer against the real
tree and re-counts. A structural check on a constant would pass the day someone adds a
second write path.

## THE FIX — bind tests live, pin baselines

| | |
|---|---|
| `research-corpus/` | **FROZEN.** Committed, hashed in `MANIFEST.json` (22 files, 312 claims, frozen 2026-08-31). Both evals resolve it through `clearance.population.frozen_dir()`, which raises rather than measure a drifted directory — and rather than return zero claims for a missing one, which `parse_corpus` did silently, from a bare cwd-relative string. |
| `research-inbox/` | **LIVE.** Where `clearance.ingest` writes. Carries a README stating the rule. No published number is computed over it. |

**Three writers had to be closed, not one.** `ingest.py` was the obvious one. `AGENTS.md`
told the fleet to append research to `research-corpus/YYYY-MM-DD-<slug>.md`, and the
`science_ingest` MCP tool description said the same — **an agent recontaminates a frozen
corpus by following prose, without touching a line of Python**, so the prose is pinned by
a control too. The manifest is generated from `git ls-files`, so it can only ever freeze
content a clean checkout also has, which is exactly the property that failed.

## THE HONEST NUMBER, WITH ITS COMMAND, ITS POPULATION AND WHEN IT WAS READ

```
$ python3 scripts/eval_citation_conflict.py                        2026-08-31 ~05:45
GOLD  fixtures/refusal-correctness/set.json  n=6  labelled 2026-08-22T21:30:00Z
  correct BASE 6/6 · CONFLICT 6/6 · ABSENCE 6/6
ATTRIBUTION  over the 27 GREEN verdicts the BASE engine produces on the frozen population,
             0 of which cite a provision at all
REGISTRY  frozen population research-corpus/ (22 files, manifest frozen 2026-08-31),
          312 claims, offline, doc cache
  BASE / CONFLICT / ABSENCE   sourced 27   refused 269   unknown 16
  CONFLICT changes 0 verdict(s) vs BASE — the corpus does not exercise it
WEDGE  WEDGE-1 REFUSED cited_provision_differs OK · WEDGE-2 SOURCED OK   (590,271 chars)

$ python3 scripts/eval_semantic_guard.py                           2026-08-31 ~05:55
GOLD  guard OFF 5/6   guard ON 6/6
REGISTRY  frozen population research-corpus/ (22 files, frozen 2026-08-31), n=312 claims
  guard OFF / ON   sourced 27   refused 269   unknown 16
  verdicts changed: 0/312 (0.0%) · RESCUED to SOURCED: 0 · respanned 8/27
```

**Every conclusion is the one the 314-run reached.** 27 cleared, 0 of them citing a
provision, 0 flips, 0 rescued. Only the denominator is now reproducible — and it is
propagated, not retyped: the front surface reads `total` from the receipt, so 312 arrives
on the page by itself. `314` survives in the docs only inside the sentences that tell the
story of how it moved.

**Proved from a clean clone, not from this tree:**

```
$ git clone --branch night/l5-semantic-guard ~/CODE/cleared /tmp/.../popclone
$ python3 scripts/freeze_population.py --check
frozen population intact: 22 files, 312 claims, frozen 2026-08-31
$ python3 -c "...parse_corpus(P.frozen_dir())"
claims parsed from a CLEAN CHECKOUT: 312
$ python3 tests/test_frozen_population.py
8/8 passed
```

## THE SWEEP — every other published figure over a directory the product writes into

| script | population | verdict |
|---|---|---|
| `eval_semantic_guard.py` | `research-corpus/` | **same defect, fixed the same way** |
| `eval_refusal_baseline.py`, `eval_refusal_ablation.py`, `eval_stats.py`, `eval_external_anchor.py` | `fixtures/refusal-correctness/set.json` | committed labelled file — clean |
| `compound_exhibit_receipt.py`, `second_subject_receipt.py`, `wedge_receipt.py` | named fixture scripts / one fetched instrument | clean |
| `boot_registry.py` | seeds the registry FROM the frozen corpus | correct by design — a cold clone should seed from frozen content |
| the registry database (`cache/refusal_log.db`, 179 rows) | grows with use | **the live shelf, by design.** It is allowed to move; wave 4 made the page name it as a different population from the corpus. |

Every remaining `glob`/`listdir` in the repo belongs to a control walking the tree, not to
a published figure.

## DEFECT 14 — AND IT WAS ON THIS LANE'S OWN FRONT PAGE

Found by rendering the page and reading it, not by reading the source. The measurement
strip said:

> That corpus cannot exercise the mechanism. **Its only evidence is the two cases above,**
> one of which is the case it was built for.

One block higher on the same page, this wave had just printed the recall boundary the
wave-4 verifier measured: **SHIPS 9/11, BASE 7/11 on 11 provision-bearing claims labelled
before the run.** Two correct blocks, one screen apart, one denying the other's existence
— on the front page of the product that exists to catch exactly that. The strip's sentence
was true when it was written and became false when the neighbour arrived. Fixed; pinned by
`t_the_measurement_strip_does_not_deny_the_heldout_probe`, RED-proved against the
committed pre-fix surface.

## DEFECT 15 — THE RED PROOF PASSED, AND THAT WAS THE FINDING

The first attempt to show defect 14's control going red **printed PASS**. Cause: the
control reads `_measurement_html()`, and with `docs/EVAL-*.json` absent that function
returns `""`, so every assertion over it passes vacuously. **Four surface controls were
one deleted file away from meaning nothing** — a green line that says nothing is the
failure mode nobody audits. Closed by
`test_the_receipt_every_surface_reads_from_is_committed`, which asserts the receipt exists
AND is tracked, so the skip branch can only fire on a broken tree.

## THE BOUNDARY, NOW ON THE SURFACE THAT SELLS THE MECHANISM

The brief required it and the page did not say it. `wedge.RECALL_BOUNDARY`, rendered on
`/front` and `/`:

> **What this refuses, and what it does not.** This check refuses this SHAPE of the error —
> a carrier clause that names a rival provision by number. It is not a claim to catch every
> miscited provision: on 11 provision-bearing claims labelled before the run it closes 9,
> against 7 for the engine without it, and the two it misses cite a rival whose subject is
> named in words rather than by numeral. Reproduce: `python3 scripts/probe_citation_heldout.py`.

Pinned by `t_every_surface_states_the_recall_boundary`, which fails if the word "shape"
or the reproduce command leaves the sentence.

## FINAL STATE, ALL RE-RUN IN THIS SESSION

```
$ for f in tests/test_*.py; do python3 $f; done      20 files, all green
    test_watch_it_go_red 72/72 · test_citation_conflict 21/21 ·
    test_front_surface 22/22 · test_frozen_population 9/9 ·
    test_semantic_guard 18/18 · test_registry_surface 13/13

Playwright device emulation, measured INSIDE the page (the --window-size trap avoided):
  /front  /registry  /   at iPhone 12 Pro   clientWidth 390  innerWidth 390  bodyScrollWidth 390
  the same three        at 1440             1440 / 1440 / 1440, zero overflowing elements
  the only boxes wider than 390 are TABLEs inside div.exhibit { overflow-x: auto },
  whose own box clips inside the viewport — a scroll container, not a page overflow.
```

Commits: `048f2cb` (the population), `d1f4b39` (the page that contradicted itself).
Branch pushed. `main` untouched. Nothing deployed, no keys, repo private.

## RE-ENTRY PROMPT — what tomorrow starts from

**Read this file's wave-4 and wave-5 sections first, then `docs/FINDING-citation-conflict-2026-08-31.md`.**

The product is honest and the instrument is now frozen. What is left is not plumbing:

1. **The held-out set is n=11 and it is the only evidence the mechanism has.** Grow it —
   near-misses, correct citations, sub-paragraph cases (`Article 99(4)(g)` vs `99(3)`),
   and a second instrument that is not the AI Act, **labelled before any run**. The
   discipline that makes it worth anything is written at the top of
   `scripts/probe_citation_heldout.py`, including the case that was withdrawn because
   *the label was wrong and the engine was right* — do that again rather than score
   against a bad label.
   ⚠️ **`clearance/wedge.py::RECALL_BOUNDARY` carries the probe's numbers as hand-typed
   literals** (9 and 7), and its control pins the word "shape" and the reproduce command,
   not the figures. Grow the probe and that sentence goes stale silently — the way "the
   other nine causes" did in wave 4. Re-type them in the same commit, or derive them from
   a committed probe receipt.
2. **Two named recall holes, both measured, both open.**
   `F3`: the gate is silent when the rival provision's subject is named in words
   (Article 101(1) clears a false Article 5 claim). `provisions("Annex III") -> []` and
   `provisions("Articles 8-17") -> [('article','8')]` — roman numerals and ranges are
   invisible, and this instrument's high-risk list IS Annex III.
3. **The corpus cannot exercise the mechanism at all** — 0 of 27 cleared claims cite a
   provision. A legal/regulatory corpus is the population that would make the REGISTRY arm
   mean something. Adding one is now a reviewed act: add files, re-run
   `python3 scripts/freeze_population.py`, commit the manifest, and say which numbers moved.
4. **Do not re-open the ABSENCE arm** without a population that can price it. It is built,
   measured, and off for the third time.

Standing rules this lane learned the hard way, in the order they cost time:
**a number is not a measurement until its population is frozen and committed** ·
**an agent recontaminates a corpus by following prose, so pin the prose** ·
**a control that can return "" is a control that can pass vacuously — assert its input exists** ·
**render the page and read it; two true sentences one screen apart can contradict each other.**
