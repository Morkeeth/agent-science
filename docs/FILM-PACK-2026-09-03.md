# Film pack — Agent Science · Agentic Cinema (record in one sitting)

**Lane T4, 2026-09-03. Hosted host `H = https://agent-science-568004190078.us-central1.run.app`. Every `expected:` block below is pasted from a REAL run tonight (2026-09-03, ~03:2x UTC). Deadline: Agentic Cinema, Tue 2026-09-09 14:00 PDT.**

**Status: INCOMPLETE at close-down.** Beats 0–4 are recorded with live output. The SOURCED-verdict-with-citation-URL beat and the 5 Devpost screenshots are NOT done — see Honest verdict at the bottom.

---

## Pre-flight (run once before recording) — all green tonight

```
show: for p in /health /partners "/visibility/ui?q=ralph+loop+agentic" /truths/ui /registry /popular/ui; do \
        curl -s -o /dev/null -w "%{http_code} %{size_download}B  $p\n" --max-time 25 \
        "https://agent-science-568004190078.us-central1.run.app$p"; done
expected:
  200 322B    /health
  200 1713B   /partners
  200 4995B   /visibility/ui?q=ralph+loop+agentic
  200 2929B   /truths/ui
  200 96226B  /registry
  200 3103B   /popular/ui

show: curl -s "$H/health"
expected: {"ok": true, "service": "agent-science", "gemini": true, "gemini_path": "vertex:hack-fleet",
           "parallel": true, "parallel_sdk": true, "parallel_sdk_version": "1.3.2",
           "agent_builder": true, "adk_version": "2.7.1", "engine_default": "adk"}
```
If `/health` is not 200 or `ok` is not true, stop and warm the service before recording.

---

## Beat 0 — the problem (0:00–0:25, 25s)

- **say:** "Every agentic builder asks the same questions and gets one summarized answer with footnotes. You cannot see what was skipped, and you cannot tell a real source from a confident guess."
- **show:** the hosted visibility page in a browser, not yet searched: `open https://agent-science-568004190078.us-central1.run.app/visibility/ui`

## Beat 1 — the refusal, on camera (0:25–1:05, 40s)

- **say:** "The rule is: if no document we read states it, we refuse — we never paraphrase or infer. So we pointed it at our own pitch headline."
- **show:**
```
curl -s -X POST "$H/clear" -H "Content-Type: application/json" \
  -d '{"script":"94% of film archives are unclearable for AI training.\n","subject":"agentic-cinema-demo"}'
```
- **expected (real, tonight):**
```
claims_extracted: 1 · sourced: 0 · unsourced: 1
row C1: "94% of film archives are unclearable for AI training."
  label: UNSOURCED
  cause: search_found_no_admissible_source
  why:  "we searched and no document we read states it"
  parallel_calls: 0 · corpus_hit: true · probe: log_hit (cross-subject)
```
- **say (the compound, free):** "It refused — and it cost zero metered calls, because we already searched this once and the shelf remembers. The second ask is free."

## Beat 2 — the verdict with transparency (1:05–1:55, 50s) — the WOW

- **say:** "Ask a live one: ralph loop agentic. It doesn't just answer — it shows the verdict, and how it searched."
- **show:** `open https://agent-science-568004190078.us-central1.run.app/visibility/ui?q=ralph+loop+agentic`
  and the JSON behind it: `curl -s "$H/visibility?q=ralph+loop+agentic"`
- **expected (real, tonight):**
```
primary.verdict: CONTRARY_TO_RESEARCH
primary.cause:   field_outruns_research
transparency keys: ['angles_searched', 'shallow_route', 'imbalance']
panel keys: query, primary, personal_prior, field, agentic_practices, peer_queries,
            optimization, parallel_probes, discipline, transparency, stack_fit, shelf_stats
```
- **say:** "CONTRARY to research — the field is ahead of the papers — plus the angles it searched and an imbalance warning. Not one answer: the whole search, shown."

## Beat 3 — the truth shelf that compounds (1:55–2:30, 35s)

- **say:** "Every claim it clears joins a shelf that grows with use, so the field's asks get cheaper over time."
- **show:** `open https://agent-science-568004190078.us-central1.run.app/truths/ui`
- **expected (real, tonight):** `Shelf: 280 claims · hit rate 0.653 · 213 queries logged`
- **NUMBER RULE:** do NOT speak a fixed shelf count — it grows between now and recording. Let the screen show whatever it is, and say "hundreds of claims, growing every time someone asks." (The claims map marks the shelf count PROVEN only as a re-curl-before-you-say-it live measure.)

## Beat 4 — the buyer, sourced (2:30–3:00, 30s)

- **say:** "One customer already owns this budget line: documentary Errors & Omissions. E&O is a flat premium of about two to ten thousand dollars per film — we cite the broker. The clearance labor underneath it is what we cancel."
- **show:** `SUBMISSION.md` §Buyer (the Sources block: Desktop Documentaries / C&S broker, Wrapbook 2025-10-01).
- **NUMBER RULE:** say the flat premium ($2,000–$10,000) only. Never the removed figures — see "Never say".

**Time budget: 0:25 + 0:40 + 0:50 + 0:35 + 0:30 = 3:00 total (beat 0 overlaps the intro).** Trim beat 2's JSON view to hold ≤3:00.

---

## Never say (UNPROVEN / removed — will fail the claims map)

- "$15k–$80k per hour-long documentary in researcher time" — removed, no primary source.
- "0.5–2% of production budget on E&O" — removed, wrong object (no source frames E&O as % of budget).
- "on camera" about the refusal being in a finished cut — nobody in this lane watched `demo/demo-final.mp4`; only say it after checking the cut (claims map P4).
- named buyers (Chubb, Beazley, BBC Studios, Participant) — removed; say "an E&O underwriter / clearance desk", no named buyer contacted.
- any fixed shelf count spoken aloud — show it, don't say a frozen number.

---

## UNFINISHED at close-down (2026-09-03)

- **The SOURCED verdict beat** (a claim that returns SOURCED with a real `citation_url` + quoted terms) was not captured — the pack shows the refusal and the CONTRARY verdict, not a green SOURCED row with its URL. Capture one before recording if a "verdict WITH its source" beat is wanted.
- **The 5 Devpost screenshots** (`docs/screenshots/`) were not taken. Intended set + the command per file: (1) `/health` 200 JSON; (2) `/visibility/ui?q=ralph+loop+agentic` panel; (3) `/truths/ui` shelf line; (4) the refusal gap report from `POST /clear`; (5) `SUBMISSION.md` §Buyer Sources block.
- All `expected:` blocks are tonight's live values; re-run the pre-flight at record time.
