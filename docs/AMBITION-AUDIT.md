# AMBITION AUDIT — 2026-08-22 evening

Lenses: hackathon judge · clearance-lead user · VC. Object: `PLAN-30` + live product.

## Verdict

The plan lists depth. The **product still sells search**. A stranger opening the hosted
URL sees a generic paste box. The company sentence — *the second production costs a
fraction* — is proven in a review receipt against a fixture, not in the thing a human
uses. That is the ambition hole.

| Lens | What they need in 60s | What they get today |
|------|----------------------|---------------------|
| **Judge** | Proof this ≠ “AI googled my script” | Citations table energy; corpus invisible |
| **Clearance lead** | Action list + memo they can forward | `<pre>` of markdown; no workflow |
| **VC** | Unit-economics story: repeat subject cheaper | Pitch claims it; UI never shows the fraction |

## What the plan gets right

- Compounding + refusal as Idea save (EYES)
- Persistent store + refusal held-out + independence (real company work)
- Sep 9 as checkpoint, not “done”

## What the plan still under-weights

1. **The product moment is A→B, not “hosted URL exists.”** Without a visible Parallel-call
   collapse on a second script, we are the modal Parallel-track entry.
2. **Gap report is the paid artifact** — it must read like a clearance memo, not a log dump.
3. **Memory must be a place**, not only a sqlite side-effect (`/tmp` dies; no subject shelf).

## Next ambitious move (this session)

**Ship the Compounding Desk:** one command + one surface where the fraction is the hero.

- `scripts/compound_exhibit.py` — Production A then B → printed receipt (the VC number)
- Hosted/local UI — clearance desk; after run, action items first; second-script lane
  locked to subject; compound strip when `corpus_hits > 0`

Firestore / Agent Builder / video stay on the plan. They are not the ambition gap.
