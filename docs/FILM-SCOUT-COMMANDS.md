# Film scout commands · Agent Science

Copy-paste for flipbook scout or terminal recording. **No video file produced here.**

## Beat 1 · Ask (agentic question)

```bash
cd ~/CODE/cleared   # or clone Morkeeth/agent-science
python3 -m clearance.stack_cli visibility "ralph loop vs vibe coding for agentic engineering" --full --no-personal
```

## Beat 2 · Transparency (what was searched)

Same command — scroll to **pane 1b**:
- `Angles searched` / route tiers
- `SHALLOW_ROUTE` yes/no
- `IMBALANCE` warning if single-source

## Beat 3 · Not just verified (field + contrary)

```bash
python3 -m clearance.stack_cli lookup "ralph loop agentic practice"
```

Expect `[CONTRARY_TO_RESEARCH]` when field outruns paper — plus full visibility panes 3–6 (★, blogs, practices, peers).

## Beat 4 · Stack-fit (magnet)

```bash
python3 -m clearance.stack_cli stack-fit "science_lookup MCP fleet agents"
```

## Beat 5 · Hosted visibility (film in browser — no CLI)

```
https://agent-science-568004190078.us-central1.run.app/visibility/ui?q=ralph+loop+agentic
https://agent-science-568004190078.us-central1.run.app/truths/ui
```

Or one command: `bash scripts/demo_truth_layer.sh`

## Beat 6 · Compound (free re-ask)

```bash
python3 -m clearance.stack_cli lookup "Directive 2012/28/EU" 
# repeat — parallel_calls should stay 0, corpus_hits rise
```

## Community notes (optional beat)

```bash
python3 -m clearance.stack_cli notes list
```

## Full gate (judge confidence)

```bash
bash scripts/full_gate.sh
```
