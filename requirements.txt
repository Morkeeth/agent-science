# The serving path is stdlib except for one thing: Agent Builder.
# The submission requires it, and a requirement is met by an agent that RUNS,
# not by a module sitting in the tree. Pinned so the deployed revision and the
# `env -u` receipt are the same software.
google-adk==2.7.1
parallel-web==1.3.2

# Hosted workspace snapshots use explicit generation preconditions.
google-cloud-storage==3.4.1
